# How to go through all the direct and non-direct subdirectories
```shell
for dir in **/
do
  echo $dir
done
```

# How to go through all the direct subdirectories
```shell
for dir in */
do
  echo $dir
done
```